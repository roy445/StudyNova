"use client";
import { useState } from "react";
import { apiPost, useApi } from "@/lib/api";
import { Badge, Button, Card, Field, Input, Select, Textarea, useToast } from "@/components/ui";
type Hub = { id: string; name: string; educationLevel: string; schoolName: string; grade: number; examNumber: string; status: string; openAt: string | null; closeAt: string | null };
export default function AdminExamHubsPage() { const toast = useToast(); const api = useApi<{ hubs: Hub[] }>("/admin/exam-hubs"); const [form, setForm] = useState({ name: "", educationLevel: "senior", schoolName: "", grade: 1, examNumber: "第一次段考", openAt: "", closeAt: "", status: "draft", announcement: "", showMarquee: false }); const [words, setWords] = useState(""); async function create() { try { await apiPost("/admin/exam-hubs", { ...form, grade: Number(form.grade), openAt: form.openAt ? new Date(form.openAt).toISOString() : null, closeAt: form.closeAt ? new Date(form.closeAt).toISOString() : null }); toast.push("success", "段考專區已建立"); setForm({ ...form, name: "", announcement: "" }); await api.reload(); } catch (err) { toast.push("error", err instanceof Error ? err.message : "建立失敗"); } } async function addWords(hubId: string) { try { const parsed = JSON.parse(words); await apiPost(`/admin/exam-hubs/${hubId}/words`, { words: Array.isArray(parsed) ? parsed : [parsed] }); toast.push("success", "正式段考單字已加入"); setWords(""); } catch (err) { toast.push("error", "請確認 JSON 格式與單字欄位"); } } return <div className="space-y-4"><header><p className="text-xs uppercase tracking-[0.2em] text-[#37d3ff]">Control Center / Exam Hub</p><h1 className="mt-1 text-2xl font-semibold">段考專區管理</h1><p className="text-sm text-muted">只有狀態為已發布且符合學生資料、開放時間的專區才會顯示在前台。</p></header><Card title="建立段考專區"><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3"><Field label="段考名稱"><Input value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} placeholder="高一第一次段考" /></Field><Field label="學制"><Select value={form.educationLevel} onChange={(e) => setForm({ ...form, educationLevel: e.target.value })}><option value="junior">國中</option><option value="senior">高中</option></Select></Field><Field label="學校（留空＝不限）"><Input value={form.schoolName} onChange={(e) => setForm({ ...form, schoolName: e.target.value })} /></Field><Field label="年級"><Select value={String(form.grade)} onChange={(e) => setForm({ ...form, grade: Number(e.target.value) })}><option value="1">一年級</option><option value="2">二年級</option><option value="3">三年級</option></Select></Field><Field label="第幾次段考／自訂"><Input value={form.examNumber} onChange={(e) => setForm({ ...form, examNumber: e.target.value })} /></Field><Field label="狀態"><Select value={form.status} onChange={(e) => setForm({ ...form, status: e.target.value })}><option value="draft">草稿</option><option value="published">已開放</option><option value="closed">已關閉</option></Select></Field><Field label="開放時間"><Input type="datetime-local" value={form.openAt} onChange={(e) => setForm({ ...form, openAt: e.target.value })} /></Field><Field label="關閉時間"><Input type="datetime-local" value={form.closeAt} onChange={(e) => setForm({ ...form, closeAt: e.target.value })} /></Field></div><Field label="公告內容"><Textarea value={form.announcement} onChange={(e) => setForm({ ...form, announcement: e.target.value })} /></Field><label className="mt-3 flex items-center gap-2 text-sm"><input type="checkbox" checked={form.showMarquee} onChange={(e) => setForm({ ...form, showMarquee: e.target.checked })} />顯示首頁跑馬燈</label><Button className="mt-3" onClick={() => void create()}>建立段考專區</Button></Card><Card title="目前段考專區"><div className="space-y-3">{api.data?.hubs.map((hub) => <div key={hub.id} className="glass-soft rounded-xl p-3"><div className="flex flex-wrap items-center justify-between gap-2"><div><p className="font-semibold">{hub.name}</p><p className="text-xs text-muted">{hub.schoolName || "不限學校"}・{hub.educationLevel === "senior" ? "高中" : "國中"}{hub.grade}・{hub.examNumber}</p></div><Badge tone={hub.status === "published" ? "green" : hub.status === "closed" ? "rose" : "muted"}>{hub.status}</Badge></div><div className="mt-3 grid gap-2 sm:grid-cols-[1fr_auto]"><Textarea value={words} onChange={(e) => setWords(e.target.value)} placeholder={'正式單字 JSON，例如：[{"word":"abandon","meaning":"放棄","partOfSpeech":"v.","synonyms":["leave"],"example":"...","exampleZh":"..."}]'} /><Button onClick={() => void addWords(hub.id)}>加入正式單字</Button></div></div>)}</div></Card></div>; }
