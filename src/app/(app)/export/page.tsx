"use client";
import { useEffect, useMemo, useState } from "react";
import { Button, Card, Field, Select, Stat } from "@/components/ui";
const DATASETS = [{ key: "notes", label: "我的專屬筆記", group: "學習資料" }, { key: "vocabulary", label: "單字", group: "學習資料" }, { key: "wrong", label: "錯題本", group: "測驗資料" }, { key: "studyMaterials", label: "AI 整理與學習資料", group: "學習資料" }, { key: "plans", label: "學習計畫", group: "學習紀錄" }, { key: "studyRecords", label: "每日學習紀錄", group: "學習紀錄" }, { key: "focus", label: "專注計時", group: "學習紀錄" }, { key: "tasks", label: "任務紀錄", group: "學習紀錄" }, { key: "dailyTasks", label: "每日任務", group: "學習紀錄" }, { key: "achievements", label: "成就與徽章", group: "學習紀錄" }, { key: "nova", label: "Nova 交易紀錄", group: "帳務紀錄" }, { key: "xp", label: "XP 紀錄", group: "帳務紀錄" }];
const FORMATS = [{ key: "json", label: "JSON", cost: 100 }, { key: "csv", label: "CSV（表格）", cost: 200 }, { key: "xlsx", label: "XLSX（Excel）", cost: 500 }, { key: "txt", label: "TXT（純文字）", cost: 100 }, { key: "md", label: "Markdown", cost: 100 }, { key: "pdf", label: "PDF（學習報告）", cost: 800 }, { key: "docx", label: "DOCX（Word）", cost: 600 }, { key: "zip", label: "ZIP（完整資料）", cost: 1000 }];
export default function ExportPage() {
  const [selected, setSelected] = useState<string[]>(["notes"]); const [format, setFormat] = useState("json"); const [from, setFrom] = useState(""); const [to, setTo] = useState(""); const [preview, setPreview] = useState<{ datasets: Array<{ label: string; count: number }>; total: number; estimatedBytes: number; sample: Array<Record<string, unknown>>; cost: number } | null>(null); const [loading, setLoading] = useState(false);
  const query = useMemo(() => `/api/v1/exports/preview?kind=${encodeURIComponent(selected.join(","))}&format=${format}${from ? `&from=${encodeURIComponent(new Date(from).toISOString())}` : ""}${to ? `&to=${encodeURIComponent(new Date(to).toISOString())}` : ""}`, [selected, format, from, to]);
  useEffect(() => { if (!selected.length) return; const timer = window.setTimeout(() => { setLoading(true); fetch(query, { credentials: "include" }).then((r) => r.json()).then((r) => setPreview(r.data)).catch(() => setPreview(null)).finally(() => setLoading(false)); }, 250); return () => window.clearTimeout(timer); }, [query, selected.length]);
  const toggle = (key: string) => setSelected((current) => current.includes(key) ? current.filter((item) => item !== key) : [...current, key]);
  const downloadUrl = selected.length ? `/api/v1/exports/download?kind=${encodeURIComponent(selected.join(","))}&format=${format}&confirmed=1${from ? `&from=${encodeURIComponent(new Date(from).toISOString())}` : ""}${to ? `&to=${encodeURIComponent(new Date(to).toISOString())}` : ""}` : "#";
  const download = () => { if (!preview || loading || !selected.length) return; const cost = preview.cost ?? 0; if (!window.confirm(`第一次確認：將匯出 ${preview.total} 筆資料，格式為 ${format.toUpperCase()}。`)) return; if (!window.confirm(`第二次確認：正式下載將扣除 ${cost} Nova，是否繼續？`)) return; window.location.assign(downloadUrl); };
  return <main className="container space-y-5 py-6 sm:py-8"><div><p className="text-xs uppercase tracking-[.24em] text-[#37d3ff]">MY DATA</p><h1 className="mt-1 text-2xl font-bold sm:text-3xl">我的資料匯出中心</h1><p className="mt-2 max-w-3xl text-sm text-muted">先預覽實際資料內容，再選擇格式。圖片或 PDF 等檔案會在預覽區顯示摘要；正式下載前會再次確認並依格式扣除 Nova。</p></div><div className="grid gap-5 lg:grid-cols-[1.2fr_.8fr]"><Card title="選擇資料" subtitle="可複選多種資料，僅能匯出目前登入帳號自己的紀錄"><div className="grid gap-2 sm:grid-cols-2">{DATASETS.map((item) => <label key={item.key} className={`flex cursor-pointer items-center gap-2 rounded-xl border p-3 text-sm transition ${selected.includes(item.key) ? "border-[#37d3ff]/60 bg-[#37d3ff]/10" : "border-[var(--line)] bg-white/[0.02]"}`}><input type="checkbox" checked={selected.includes(item.key)} onChange={() => toggle(item.key)} className="accent-[#37d3ff]" /><span><strong>{item.label}</strong><small className="mt-0.5 block text-[10px] text-muted">{item.group}</small></span></label>)}</div><div className="mt-4 grid gap-3 sm:grid-cols-2"><Field label="開始日期" hint="留空代表不限"><input type="date" value={from} onChange={(e) => setFrom(e.target.value)} className="w-full rounded-xl border border-[var(--line)] bg-black/20 px-3 py-2" /></Field><Field label="結束日期" hint="留空代表不限"><input type="date" value={to} onChange={(e) => setTo(e.target.value)} className="w-full rounded-xl border border-[var(--line)] bg-black/20 px-3 py-2" /></Field></div></Card><Card title="格式與匯出" subtitle="先看內容預覽，再進行兩次確認"><Field label="匯出格式"><Select value={format} onChange={(e) => setFormat(e.target.value)}>{FORMATS.map((item) => <option key={item.key} value={item.key}>{item.label}・{item.cost} Nova</option>)}</Select></Field>{preview && <div className="mt-4 grid grid-cols-2 gap-2"><Stat label="資料筆數" value={preview.total} tone="cyan" /><Stat label="預估大小" value={`${(preview.estimatedBytes / 1024).toFixed(1)} KB`} /></div>}<div className="mt-4 rounded-xl border border-[#37d3ff]/20 bg-[#37d3ff]/5 p-3 text-xs leading-5 text-muted">{loading ? "正在建立預覽…" : preview ? `包含 ${preview.datasets.filter((item) => item.count > 0).map((item) => `${item.label} ${item.count} 筆`).join("、") || "目前沒有符合條件的資料"}。` : "選擇資料後會顯示預計筆數與大小。"}</div>{selected.length > 0 && preview && !loading ? <Button full variant="gold" onClick={download}>預覽完成，確認下載（扣 {preview.cost} Nova）</Button> : <Button full disabled>{loading ? "等待預覽完成…" : "請先選擇資料"}</Button>}</Card></div>{preview?.sample?.length ? <Card title={format === "pdf" ? "PDF 報告預覽" : "匯出內容預覽"} subtitle="以下為前幾筆實際內容，正式檔案會包含上方選取範圍內的全部資料"><div className={format === "pdf" ? "rounded-lg bg-white p-5 text-slate-900 shadow-inner" : "max-h-72 overflow-auto rounded-lg bg-black/20 p-3"}>{preview.sample.map((row, index) => <pre key={index} className="mb-3 whitespace-pre-wrap border-b border-slate-200/20 pb-2 text-xs last:mb-0 last:border-0">{JSON.stringify(row, null, 2)}</pre>)}</div></Card> : null}<Card title="匯出安全說明"><div className="grid gap-2 text-xs leading-5 text-muted sm:grid-cols-3"><p>預覽不扣 Nova；只有第二次確認後的正式下載會扣點。</p><p>PDF、DOCX、XLSX、ZIP 等格式依複雜度收取不同 Nova。</p><p>不會包含 System Prompt、API Key、Token 或內部工具資訊。</p></div></Card></main>;
}
